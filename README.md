# Portiga

Para criar o venv e conseguir rodar o projeto (na raíz do projeto)

Bash
```
python -m venv .venv
```

Para ativar a venv

Bash
```
venv/Scripts/activate
```

Para instalar o FreeSimpleGUI (necessário para interface gráfica)

Bash
```
pip install FreeSimpleGUI
```
